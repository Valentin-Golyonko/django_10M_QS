psycopg[binary]

Django==4.2b1
djangorestframework

ipython
requests
python-dotenv

gunicorn

locust
django-debug-toolbar

# INSTALL/UPDATE REQUIREMENTS
# pip install -U pip setuptools wheel pip-tools --timeout 50
# pip-compile --resolver=backtracking
# pip-sync
# pip install -U black django-debug-toolbar coverage tblib locust --timeout 50
# pip install -r requirements.txt --timeout 50

# CODE FORMATTER
# black .
